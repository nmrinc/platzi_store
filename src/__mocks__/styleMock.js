module.exports = {};

/**
 * @concept Mock for stylo styles
 * @context As enzyme can't work with this kind of files, it's necessary to create a mock empty file, and pass it
 * @context to jest configuration to use this mock on every .styl or .css file needed to render the component.
 */
